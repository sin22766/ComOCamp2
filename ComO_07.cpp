#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n - 1; ++i) {
        pair<string, string> chain;
        cin >> chain.first >> chain.second;
        if (i == 0) {
            cin >> chain.first >> chain.second;
        } else {

        }
    }
    return 0;
}
